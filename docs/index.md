# paired_sc

Advanced paired tissue scRNA-seq workflows with a package-first and CLI-first
interface.

The package is designed around:

- reproducible manifest-driven input contracts
- donor-aware analysis instead of cell-only pseudo-replication
- first-class domains for advanced analyses
- a clean separation between reusable workflow code and paper-specific logic


